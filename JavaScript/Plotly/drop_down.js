function submitForm() {
    // Get the selected value from the dropdown
    const selectedCar = document.getElementById("cars").value;
  
    // Display an alert with the selected value (you can replace this with your desired action)
    alert(`Selected car: ${selectedCar}`);